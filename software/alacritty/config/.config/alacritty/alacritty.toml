# https://alacritty.org/config-alacritty.html

###########
# GENERAL #
###########

[general]

# Import additional configuration files
import = [
  # uncomment the flavour you want below:
  # "~/.config/alacritty/themes/catppuccin-latte.toml"
  # "~/.config/alacritty/themes/catppuccin-frappe.toml"
  # "~/.config/alacritty/themes/catppuccin-macchiato.toml"
  # "~/.config/alacritty/themes/catppuccin-mocha.toml"
  "~/.config/alacritty/themes/gnome-terminal.toml"
]

# Shell program and its arguments
# Default for Linux/BSD/macOS is $SHELL or user's login shell
# Default for Windows is "powershell"
#[shell]
#program = "/bin/zsh"  # Example for Linux/BSD/macOS
#args = ["-l"]

# Working directory
working_directory = "None"  # Default: "None"

# Live config reload (changes require restart)
live_config_reload = true  # Default: true

# Offer IPC using alacritty msg (unix only)
ipc_socket = false  # Default: true

#######
# ENV #
#######

[env]

# Needed for $HOME/.vimrc
TERM = "xterm-256color"

##########
# WINDOW #
##########

[window]

# Window dimensions
dimensions = { columns = 0, lines = 0 }  # Default: columns = 0, lines = 0

# Window startup position
position = "None"  # Default: "None"

# Padding
padding = { x = 10, y = 10 }  # Default: x = 0, y = 0

# Dynamic padding
dynamic_padding = false  # Default: false

# Window decorations
decorations = "Full"  # Default: "Full"

# Opacity
opacity = 1.0  # Default: 1.0

# Blur (macOS/KDE Wayland only)
blur = false  # Default: false

# Startup mode
startup_mode = "Maximized"  # Default: "Windowed"

# Window title
title = "darkstar"  # Default: "Alacritty"

# Dynamic title
dynamic_title = true  # Default: true

# Window class (Linux/BSD only)
class = { instance = "darkstar", general = "darkstar" }  # Default: instance = "Alacritty", general = "Alacritty"

# Decorations theme variant
decorations_theme_variant = "Dark"  # Default: "None"

# Resize increments
resize_increments = false  # Default: false

# Option as Alt (macOS only)
option_as_alt = "OnlyLeft"  # Default: "None"

#############
# SCROLLING #
#############

[scrolling]

# Maximum number of lines in the scrollback buffer
history = 0  # Default: 10000

# Number of lines scrolled for every input scroll increment
multiplier = 1  # Default: 3

########
# FONT #
########

[font]

# Normal font style
normal = { family = "MesloLGM Nerd Font Mono", style = "Regular" }  # Default for Linux/BSD: "monospace", Regular

# Bold font style
bold = { style = "Bold" }  # Inherits family from normal, Default style: Bold

# Italic font style
italic = { style = "Italic" }  # Inherits family from normal, Default style: Italic

# Bold Italic font style
bold_italic = { style = "Bold Italic" }  # Inherits family from normal, Default style: Bold Italic

# Font size in points
size = 12  # Default: 11.25

# Offset is extra space around each character
offset = { x = 0, y = 2 }  # Default: x = 0, y = 0

# Glyph offset determines the locations of the glyphs within their cells
glyph_offset = { x = 0, y = 0 }  # Default: x = 0, y = 0

# Use built-in font for box drawing characters
builtin_box_drawing = true  # Default: true

##########
# COLORS #
##########

[colors]

# Transparent background colors
transparent_background_colors = false  # Default

# Draw bold text with bright colors
draw_bold_text_with_bright_colors = true  # Default false

# Primary colors
[colors.primary]
#foreground = "#d8d8d8"  # Default
#background = "#181818"  # Default
#dim_foreground = "#828482"  # Default
#bright_foreground = "None"  # Default

# Cursor colors
[colors.cursor]
#text = "CellBackground"  # Default
#cursor = "CellForeground"  # Default

# Vi mode cursor colors
[colors.vi_mode_cursor]
#text = "CellBackground"  # Default
#cursor = "CellForeground"  # Default

# Search colors
[colors.search.matches]
#foreground = "#181818"  # Default
#background = "#ac4242"  # Default

[colors.search.focused_match]
#foreground = "#181818"  # Default
#background = "#f4bf75"  # Default

# Hint colors
[colors.hints.start]
#foreground = "#181818"  # Default
#background = "#f4bf75"  # Default

[colors.hints.end]
#foreground = "#181818"  # Default
#background = "#ac4242"  # Default

# Line indicator colors
[colors.line_indicator]
#foreground = "None"  # Default
#background = "None"  # Default

# Footer bar colors
[colors.footer_bar]
#foreground = "#181818"  # Default
#background = "#d8d8d8"  # Default

# Selection colors
[colors.selection]
#text = "CellBackground"  # Default
#background = "CellForeground"  # Default

# Normal colors
[colors.normal]
#black = "#181818"  # Default
#red = "#ac4242"  # Default
#green = "#90a959"  # Default
#yellow = "#f4bf75"  # Default
#blue = "#6a9fb5"  # Default
#magenta = "#aa759f"  # Default
#cyan = "#75b5aa"  # Default
#white = "#d8d8d8"  # Default

# Bright colors
[colors.bright]
#black = "#6b6b6b"  # Default
#red = "#c55555"  # Default
#green = "#aac474"  # Default
#yellow = "#feca88"  # Default
#blue = "#82b8c8"  # Default
#magenta = "#c28cb8"  # Default
#cyan = "#93d3c3"  # Default
#white = "#f8f8f8"  # Default

# Dim colors
[colors.dim]
#black = "#0f0f0f"  # Default
#red = "#712b2b"  # Default
#green = "#5f6f3a"  # Default
#yellow = "#a17e4d"  # Default
#blue = "#456877"  # Default
#magenta = "#704d68"  # Default
#cyan = "#4d7770"  # Default
#white = "#8e8e8e"  # Default

########
# BELL #
########

[bell]

# Visual bell animation effect
animation = "Linear"  # Default: "Linear"

# Duration of the visual bell flash in milliseconds
duration = 0  # Default: 0

# Visual bell animation color
color = "#ffffff"  # Default: "#ffffff"

# Command executed when the bell is rung
command = "None"  # Default: "None"

#############
# SELECTION #
#############

[selection]

# Characters that are used as separators for "semantic words" in Alacritty
semantic_escape_chars = ",│`|:\"' ()[]{}<>\t"  # Default: ",│`|:\"' ()[]{}<>\t"

# When set to true, selected text will be copied to the primary clipboard
save_to_clipboard = true  # Default: false

##########
# CURSOR #
##########

[cursor]

# Cursor style
style = { shape = "Block", blinking = "On" }  # Default: shape = "Block", blinking = "Off"

# Vi mode cursor style (falls back to the active value of the normal cursor if "None")
vi_mode_style = "None"  # Default: "None"

# Cursor blinking interval in milliseconds
blink_interval = 750  # Default: 750

# Time after which cursor stops blinking, in seconds
blink_timeout = 0  # Default: 5

# When true, the cursor will be rendered as a hollow box when the window is not focused
unfocused_hollow = true  # Default: true

# Thickness of the cursor relative to the cell width
thickness = 0.15  # Default: 0.15

############
# TERMINAL #
############

[terminal]

# Controls the OSC 52 behavior for clipboard interactions
osc52 = "CopyPaste"  # Default: "OnlyCopy"

#########
# MOUSE #
#########

[mouse]

# When true, the cursor is temporarily hidden when typing
hide_when_typing = true  # Default: false

# Mouse bindings (actual bindings need to be defined as per user preference)
# Example: bindings = [{ mouse = "Left", action = "Paste" }]
bindings = [
  { mouse = "Middle", action = "Paste" }
]

#########
# HINTS #
#########

[hints]

# Define the keys used for hint labels
alphabet = "jfkdls;ahgurieowpq"  # Default: "jfkdls;ahgurieowpq"

# Enable specific hints
#[[hints.enabled]]
# Example configuration (actual settings to be defined based on user preference)
# regex = "..."
# hyperlinks = true
# post_processing = true
# persist = false
# action = "..."
# command = "..."
# binding = { key = "...", mods = "...", mode = "..." }
# mouse = { mods = "...", enabled = true }

# KEYBOARD section of Alacritty configuration

[keyboard]

# Define keyboard bindings here
# Example placeholder (actual bindings need to be defined based on user preference)
bindings = [
  # { key = "Key", mods = "Modifiers", action = "Action" },
  # { key = "Key", mods = "Modifiers", chars = "Characters to send" },
  #
  # windows and tabs
  {key = "N", mods = "Control|Shift", action = "CreateNewWindow"},
  {key = "T", mods = "Control|Shift", action = "CreateNewTab"},
  {key = "PageUp",   mods = "Control", action = "SelectPreviousTab"},
  {key = "PageDown", mods = "Control", action = "SelectNextTab"},
  {key = "1", mods = "Alt", action = "SelectTab1"},
  {key = "2", mods = "Alt", action = "SelectTab2"},
  {key = "3", mods = "Alt", action = "SelectTab3"},
  {key = "4", mods = "Alt", action = "SelectTab4"},
  {key = "5", mods = "Alt", action = "SelectTab5"},
  {key = "6", mods = "Alt", action = "SelectTab6"},
  {key = "7", mods = "Alt", action = "SelectTab7"},
  {key = "8", mods = "Alt", action = "SelectTab8"},
  {key = "9", mods = "Alt", action = "SelectTab9"},
  {key = "F11", mods = "None", action = "ToggleFullscreen"},
  # copy/paste
  {key = "V", mods = "Control|Shift", action = "Paste"},
  {key = "C", mods = "Control|Shift", action = "Copy"},
  {key = "Insert", mods = "Control", action = "CopySelection"},
  {key = "Insert", mods = "Shift", action = "PasteSelection"},
  # scrolling
  {key = "PageUp", mods = "Shift", action = "ScrollPageUp"},
  {key = "PageDown", mods = "Shift", action = "ScrollPageDown"},
  {key = "Up", mods = "Control|Shift", action = "ScrollLineUp"},
  {key = "Down", mods = "Control|Shift", action = "ScrollLineDown"},
  {key = "End", mods = "Shift", action = "ScrollToBottom"},
  {key = "Home", mods = "Shift", action = "ScrollToTop"},
  # implements all the shortcuts that require the alt key in a bash environment
  # { key = "A", mods = "Alt", chars = "\u001BA" },  # Alt + A (Move to the beginning of the line)
  # { key = "E", mods = "Alt", chars = "\u001BE" },  # Alt + E (Move to the end of the line)
  # { key = "K", mods = "Alt", chars = "\u001BK" },  # Alt + K (Kill the line from cursor to end)
  # { key = "H", mods = "Alt", chars = "\u001BH" },  # Alt + H (Delete the character before the cursor)
  # { key = "X", mods = "Alt", chars = "\u001BX" },  # Alt + X (Delete the character after the cursor)
  # { key = "D", mods = "Alt", chars = "\u001BD" },  # Alt + D (Delete the word after the cursor)
  # { key = "Backspace", mods = "Alt", chars = "\u001B\u007F" },  # Alt + Backspace (Delete word before cursor)
  # { key = "U", mods = "Alt", chars = "\u001BU" },  # Alt + U (Uppercase from cursor to end of word)
  # { key = "L", mods = "Alt", chars = "\u001BL" },  # Alt + L (Lowercase from cursor to end of word)
  # { key = "C", mods = "Alt", chars = "\u001BC" },  # Alt + C (Capitalize the current word)
  # { key = ".", mods = "Alt", chars = "\u001B." },  # Alt + . (Insert the last argument of previous command)
  # { key = "Y", mods = "Alt", chars = "\u001BY" },  # Alt + Y (Yank the top of the kill-ring)
  # { key = "/", mods = "Alt", chars = "\u001B/" },  # Alt + / (Attempt completion)
  # { key = "T", mods = "Alt", chars = "\u001BT" },  # Alt + T (Transpose the words around cursor)
  # { key = "R", mods = "Alt", chars = "\u001BR" },  # Alt + R (Recall the last command that matches input)
  # { key = "N", mods = "Alt", chars = "\u001BN" },  # Alt + N (Search forward through history)
  # { key = "P", mods = "Alt", chars = "\u001BP" },  # Alt + P (Search backward through history)
  # { key = "Q", mods = "Alt", chars = "\u001BQ" }   # Alt + Q (Not a standard bash shortcut, customizable)
  { key = "Left", mods = "Alt", chars = "\u001BB" },  # Alt + B (Move backward one word)
  { key = "Right", mods = "Alt", chars = "\u001BF" },  # Alt + F (Move forward one word)
]
