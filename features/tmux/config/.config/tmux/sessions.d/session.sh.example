# Copy this file to NAME.sh. The filename becomes the tmux session name.

TMUX_SESSION_DIRECTORY="$HOME/workspace/example"
TMUX_SESSION_COMMAND='nvim'

# For a multi-window session, remove TMUX_SESSION_COMMAND and define this hook.
# The hook must create a detached session named "$1" and must not attach to it.
# tmux_session_create() {
#   session_name=$1
#   session_directory=$2
#
#   tmux new-session -d -s "$session_name" -n editor -c "$session_directory"
#   tmux send-keys -t "$session_name:editor" nvim C-m
#   tmux new-window -t "$session_name" -n shell -c "$session_directory"
#   tmux select-window -t "$session_name:editor"
# }