" TMUX ------------------------------------------------------------------- {{{

" Italic in vim when using tmux
if $TERM =~ 'screen-256color'
  set t_ZH=^[[3m
  set t_ZR=^[[23m
  " set t_so=^[[7m
endif

" ------------------------------------------------------------------------ }}}
