#!/usr/bin/env bash
set -e
set -u
set -o pipefail

# Linux
# find $1 -type f -print0 | xargs -0 stat --format '%Y :%y %n' | sort -nr | cut -d: -f2- | head

# Mac
find $1 -type f -exec stat -f "%m %N" "{}" \; | sort -nr | head
